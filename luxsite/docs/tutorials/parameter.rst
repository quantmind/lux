.. _parameter:

================
Parameters
================

Parameters are the provides a way to customise a lux application. They are
specified in the configuration file (``config.py`` by default) of a lux project
as discussed in the :ref:`project layout <project-layout>` tutorial.

Parameters are specified as :ref:`extension <writing-extensions>` class
attributes via the :class:`lux.core.Parameter` class.
