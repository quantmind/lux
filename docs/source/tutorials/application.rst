.. _application:

==================
Lux Application
==================

A :class:`lux.core.App` is the application which runs a web server.
It is fully defined by the extensions listed in the application configuration
script.

Accessing the application
=============================

The application can be accessed via the ``app`` attribute of a client
``request`` class.
