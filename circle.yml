machine:

  node:
    version: 5.7.0

  services:
    - redis

test:
  pre:
    - pyenv global 3.5.1
    - pip install -r requirements-dev.txt

database:
  pre:
    - psql -U postgres -f tests/db.sql

