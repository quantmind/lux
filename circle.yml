machine:

  node:
    version: 5.7.0

  services:
    - redis


dependencies:
  pre:
    - pyenv global 3.5.1
    - pip install -r requirements-dev.txt

test:
  override:
    - setup.py test --coverage -q
    - flake8


database:
  pre:
    - psql -U postgres -f tests/db.sql

